# Local Project Rules (demo-target only)
- All code must pass `npm run lint` before commit (run from `demo-target/`).
- Use ES6 modules (import/export) instead of CommonJS (require).
- Error messages must be logged using the internal `Logger` utility.
